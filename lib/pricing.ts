// Aucun prix public n'existe dans le dépôt (voir CLAUDE.md, "Questions encore
// ouvertes"). Ce flag masque tout affichage de tarif tant qu'aucune valeur
// réelle n'est confirmée par Nicolas — à passer à true le jour où une source
// de prix fiable existe (ex. tarifs Superhote exposés via API).
export const showPrices = false;
